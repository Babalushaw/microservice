# changing server port
server:
  port: 9092
  # data base configuration:MySql
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel
    username: root
    password: Babalu@21
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        dialect.storage_engine: innodb
